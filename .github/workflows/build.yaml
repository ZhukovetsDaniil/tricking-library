name: Build Application

on: push

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - name: Pull code
          uses: actions/checkout@v2

        - name: Print current directory
          run: pwd

        - name: List Files
          run: ls -a

        - name: Check dotnet
          run: dotnet

        - name: Check npm
          run: npm